"""
Purpose: Handles external communication via FastAPI and LangServe.
This module defines the REST endpoints and request processing logic.

Relations:
- Exposes the agent defined in `backend.agent`.
- Uses `backend.models` for request/response validation (Pydantic models).
"""
